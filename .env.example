# AI PoC Planner does not load this file automatically.
#
# Configure the provider and model capability profile from the Streamlit
# 「模型設定」 page. Do not put a real API key in this file or commit one.
# The local runtime creates its own SQLite/profile state under the selected
# Local or UAT runtime root.

# Optional process-level readiness policy (default: 60, allowed: 1-300).
# AI_POC_PLANNER_PROVIDER_READINESS_TIMEOUT_SECONDS=60

# Real-provider UAT is opt-in and is intentionally not enabled here.
# Keep provider-specific secrets and live-test flags out of committed config.
